apply plugin: 'java'

version = '1.0'

repositories {
    jcenter()
}

jar {
    baseName = 'deep-copy'
    version =  '0.0.1'
}

dependencies {
    testCompile 'junit:junit:4.12'
}

compileJava {
    options.compilerArgs << '-XDignore.symbol.file'
}